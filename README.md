⚠️ This project is under active development. 
Initially built with Claude Code as part of an AI tool comparison.
Available features are functional, but data is currently stored locally. 
More features including cloud sync and UI updates coming in a future iteration. 

Looking forward to making the updates!
